route:
  group_by: ["alertname", "job"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: "devsick-webhook"

receivers:
  - name: "devsick-webhook"
    webhook_configs:
      - url: "http://backend:8000/api/alerts/webhook"
        send_resolved: true
